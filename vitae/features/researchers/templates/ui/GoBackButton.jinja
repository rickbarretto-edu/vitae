{#def current_page: int #}

{#

    Go Back Button
    --------------

    A button to navigate back to the previous page in paginated searches.
    It only renders if the current page is greater than 1.

    `current_page`: the current page number of the search results.

    Content
    -------
    Used as the button's tooltip (`title` attribute).

    Usage
    -----

        <ui.GoBackButton current_page={{2}}>Back</ui.GoBackButton>

    This button will not render on the first page (`current_page` = 1).

#}


{% if current_page > 1 %}
    <a href="javascript:void(0)" onclick="history.back()" title="{{content}}"
        class="fixed bottom-5 left-5 z-50 
            bg-white text-purple-600 w-12 h-12 
            flex items-center justify-center rounded 
            hover:bg-gray-100">
        &#8592
    </a>
{% endif %}