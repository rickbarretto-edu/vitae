{# def 
    id: str,
    for_param: str
#}

{#

    Next Page Button
    ----------------

    A button component that increments a specified URL parameter and reloads the page,
    typically used for pagination or step navigation.

    `id`: the HTML id for this button element.
    `for_param`: the URL parameter to increment on click.

    Content
    -------
    The button label or inner content.

    Usage
    -----

    For example, to increment the `page` parameter starting from `?page=1`:

        <NextPageButton id="next-page" for-param="page">
            Next Page
        </NextPageButton>

    Clicking will navigate to `?page=2`, and so forth.

    Similarly, for a step parameter:

        <NextPageButton id="next-step" for-param="step">
            Step {{current_step + 1}}
        </NextPageButton>

#}


<a id={{id}} href="#" class="block w-full max-w-[16rem]">
    <form.Button>{{ content }}</form.Button>
</a>

<script>
document.getElementById('{{id}}').addEventListener('click', function (e) {
    e.preventDefault();

    const url = new URL(window.location.href);
    const params = url.searchParams;

    let currentPage = parseInt(params.get("{{for_param}}")) || 1;
    params.set("{{for_param}}", currentPage + 1);

    url.search = params.toString();
    window.location.href = url.toString();
});
</script>
