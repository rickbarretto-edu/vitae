{#def
    on_url: str,
    available: list[str] = []
#}

<form.Select on_url={{on_url}}>
    <option value="">{{content}}</option>
    {% for country in available %}
        <option value="{{ country }}">
            {{ country | title }}
        </option>
    {% endfor %}
</form.Select>