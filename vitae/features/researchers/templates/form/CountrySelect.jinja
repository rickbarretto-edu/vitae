{#def
    placeholder: str,
    on_url: str,
    available: list[str] = []
#}

<form.Select label={{content}} on_url={{on_url}}>
    <option value="">{{placeholder}}</option>
    {% for country in available %}
        <option value="{{ country }}">
            {{ country | title }}
        </option>
    {% endfor %}
</form.Select>